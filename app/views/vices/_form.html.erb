<%= form_for(@vice) do |f| %>
    
    <div class="field">
        <%= f.collection_select(:id, Vice.all, :id, :name) %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>

<% end %>